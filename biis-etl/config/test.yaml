database:
  driver: "com.microsoft.sqlserver.jdbc.SQLServerDriver"
  host: "localhost"
  port: 1433
  name: "biis_test"
  schema: "dbo"
  user: "sa"
  password: "TestP@ssw0rd!"

secrets:
  provider: "local"

notifications:
  provider: "log"
  recipients: "test@test.com"

paths:
  landing: "/tmp/biis/landing"
  staging: "/tmp/biis/staging"
  archive: "/tmp/biis/archive"

sftp:
  host: "localhost"
  port: 2222
  user: "testuser"
  password: "testpass"
  routes:
    nih_cpm: "/outbound"
    nih_les: "/outbound"
    oig: "/outbound"
    cdc: "/outbound"
    fda: "/outbound"
    afps: "/outbound"
