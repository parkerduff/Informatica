# Core PySpark runtime
pyspark==3.5.1
pandas==2.2.2
pyyaml==6.0.1

# Transfers
paramiko==3.4.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0
pytest-benchmark==4.0.0
chispa==0.10.1

# Lint / type-check
flake8==7.1.0
mypy==1.10.1

# Optional DB driver for SQL Server (production / docker-compose backend).
# The default "test" environment uses the bundled SQLite backend and does NOT
# require this package or a system ODBC driver.  Install with:
#   pip install pyodbc==5.1.0
# (requires unixodbc-dev system headers)
# distutils shim for Python 3.12+ (PySpark pandas interop)
setuptools>=69.5.1
