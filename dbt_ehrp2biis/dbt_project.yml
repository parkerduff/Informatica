name: 'dbt_ehrp2biis'
version: '1.0.0'
config-version: 2

profile: 'ehrp2biis'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  ehrp_database: 'EHRP_DB'
  ehrp_schema: 'EHRP'
  nknight_schema: 'NKNIGHT'
  info_target_schema: 'INFO_TARGET_DEV'

models:
  dbt_ehrp2biis:
    staging:
      +materialized: table
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
